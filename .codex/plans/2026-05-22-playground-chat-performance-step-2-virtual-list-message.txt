请接手 E:\AII\ugk-pi 的 Playground chat performance Step 2：Virtualize Conversation List Rows。

当前基线：
- Step 1 已完成。
- 本步骤现已完成在 4d32d42，并由 d867465 修复。

本轮只做：
- 对会话列表做虚拟滚动。
- 保证任意时刻 DOM shell count 有界。
- 桌面 row pitch 与 CSS 对齐为 60px。
- 移动 row pitch 与真实移动样式对齐为 100px。
- renderConversationListInto 必须用清 DOM 前保存的 scrollTop 计算 virtual window。
- rAF 调度不能 cancel 后吞掉 render。

禁止做：
- 不改 catalog refresh 策略。
- 不改 chat API。
- 不改非聊天面板加载策略。
- 不提交无关脏文件，不使用 git add -A。

验证：
- node --test --import tsx test/playground-conversations-controller.test.ts
- node --test --import tsx test/playground-styles.test.ts
- node --test --import tsx test/server.test.ts
- npx tsc --noEmit
- git diff --check
- 浏览器验证 desktop/mobile 滚动后 topSpacer 非 0 且 DOM count 有界。

提交建议：
- perf(playground): virtualize conversation list rows
- fix(playground): repair conversation virtual scrolling
