请接手 E:\AII\ugk-pi，执行 `/playground/conn` 性能优化 Task 5。

计划文件：
- `.codex/plans/2026-05-22-playground-conn-performance-plan.md`

本步目标：
- Split Rendering Into Targeted Updates
- 减少 `renderAll()` 的滥用，把小状态变化限制在局部渲染。

当前真实背景：
- `handlePause()`、`handleResume()`、`handleDelete()` 仍有多次 `renderAll()`。
- `handleMarkAllRead()` 成功后 `renderAll()`，随后还调用了不存在的 `loadRuns(state.selectedId)`。
- 这一步顺手修掉这个 stale call；这不是扩范围，这是把地雷从用户脚边拿开。

必须先读：
- AGENTS.md
- docs/playground-current.md
- .codex/plans/2026-05-22-playground-conn-performance-plan.md
- src/ui/conn-page-js.ts
- src/ui/conn-page-css.ts
- test/server.test.ts
- test/conn-page-ui.test.ts

实现要求：
- 拆出或明确使用 targeted render helper，例如 stats-only、list-only、selected-detail、run-history。
- `renderAll()` 保留给 initial load 和重大 fallback。
- 操作 handler 中用 targeted render 替换重复 `renderAll()`。
- `handleMarkAllRead()` 不得再引用不存在的 `loadRuns()`；改用 Task 2/3 建立的 runs loader/refresh helper。
- 保留 selected conn、expanded run、scroll position、移动端 list/detail panel 状态。
- async skills？没有 skills，这里是 conn。别把 agents 页面那套脑补进来。
- async conn 操作要有 selectedId/local connId guard，避免旧任务结果画到新面板。
- 不做大视觉重构，不改变 API/worker 行为。

测试先行：
- 增加测试或脚本断言，证明 pause/resume/delete/read-all 不再前后多次调用 `renderAll()`。
- 增加测试证明 `handleMarkAllRead()` 不再包含 `loadRuns(`。
- 覆盖 pause/resume/run-now/delete/read-all 至少一类关键路径的可见状态更新。
- 覆盖 selectedId guard 或局部 connId 捕获。

验证：
- `node --test --import tsx test/server.test.ts`
- `node --test --import tsx test/conn-page-ui.test.ts`
- `npx tsc --noEmit`
- `git diff --check`
- 浏览器操作 pause/resume/run-now/delete/read-all，不应出现重复按钮、stale selected row、滚动跳动

提交：
- 一个 Task 一个 commit
- 建议 commit message: `perf(conn): render targeted task updates`
- 不要提交无关文件

交付报告请按计划文件 Delivery Report Template 回复。
