请接手 E:\AII\ugk-pi 的 Playground chat performance Step 1：Stop Rendering Hidden Duplicate Conversation Lists。

当前基线：
- 以当时 HEAD 为准；本步骤现已完成在 9c95ac8。

本轮只做：
- 避免 desktop/mobile 两份完整会话列表同时挂载 DOM。
- 桌面只渲染 desktopConversationList，清空 mobileConversationList。
- 移动抽屉关闭时清空双列表。
- 移动抽屉打开时只渲染 mobileConversationList。
- resize 跨 641px 断点时触发 renderConversationDrawer。

禁止做：
- 不做虚拟滚动。
- 不改会话 API。
- 不改 Team Runtime、conn、worker、scheduler。
- 不提交无关脏文件，不使用 git add -A。

验证：
- node --test --import tsx test/playground-conversations-controller.test.ts
- node --test --import tsx test/server.test.ts
- npx tsc --noEmit
- git diff --check

提交建议：
- perf(playground): avoid hidden duplicate conversation list rendering
