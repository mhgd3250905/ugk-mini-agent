请接手 E:\AII\ugk-pi，执行 /playground/agents 性能优化 Task 1。

计划文件：
- .codex/plans/2026-05-22-playground-agents-performance-plan.md

本步目标：
- Remove Duplicate Initial Main Skills Request
- 首屏不要重复请求 GET /v1/agents/main/skills。

真实测试背景：
- /playground/agents 首屏测到两次 main skills：
  - GET /v1/agents/main/skills: 162.9ms
  - Duplicate GET /v1/agents/main/skills: 129.1ms
- /v1/agents 和 /v1/agents/status 很快，真正浪费在重复 skills 请求。

必须先读：
- AGENTS.md
- docs/playground-current.md
- src/ui/agents-page.ts
- src/routes/agent-profiles.ts
- src/agent/agent-profile-catalog.ts
- src/agent/agent-service-registry.ts
- test/server.test.ts
- test/playground-agent-switch.test.ts
- 相关 test/agent-profile*.test.ts

实现要求：
- 复用 gallery skills 结果作为 skillsByAgentId.main，或拆分 gallery metadata 与 scoped skill list。
- main agent 首次选中时不得再次请求同一个 endpoint。
- installable skill picker 仍然能看到 disabled/available entries。
- 手动刷新仍能有意识地刷新 agent summaries 和 gallery skills。
- 不改变 Agent runtime、profile 创建/归档、技能 enable/disable 语义。

测试先行：
- 测试初始脚本/路径不会 fetch /v1/agents/main/skills 两次。
- 测试 gallery load 后选择 main 使用缓存。

验证：
- node --test --import tsx test/server.test.ts
- node --test --import tsx test/playground-agent-switch.test.ts
- npx tsc --noEmit
- git diff --check
- 浏览器打开 /playground/agents，waterfall 只应有一次 /v1/agents/main/skills。
- 技能数量和 installable skill dropdown 仍正常。

提交：
- 一个 Task 一个 commit。
- 建议 commit message: perf(agents): reuse main skills on initial load
- 不要提交 .env、.data、runtime/*、public/*、截图、browser profile、curate_news*.py、无关 .codex/plans。

交付报告请按计划文件 Delivery Report Template 回复。
