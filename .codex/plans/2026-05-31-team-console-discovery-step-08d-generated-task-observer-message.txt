请接手 `E:\AII\ugk-pi` 的 Team Console Discovery Step 08D：generated Task run controls and observer integration。

当前基线：
- 分支：`main...origin/main [ahead 6]`
- 最新 commit：`af0362a docs(team-console): record task chain validation`
- 当前 Discovery 已完成到 Step 08C：5174 root Discovery Task 菜单有 `Discovery 子画布` toggle，subcanvas 只显示非 archived generated child catalog，generated child 仍不进入 root canvas。
- Step 08C 已由 Codex 复核：focused tests、Team Console tsc/build/full tests、top-level `npx tsc --noEmit`、`git diff --check`、5174 mock browser verification 均通过。
- 暂存区应为空；不要 stage，不要 commit。
- 既有 dirty 边界：`.pi/skills/anthropics/skill-creator/**` tracked deletions 不是本轮内容，别恢复、别 stage；`.pi/skills/skill-creator/`、`.codex/plans/*`、public/runtime 报告产物、临时 HTML/脚本都不要提交。

必须先读：
- `AGENTS.md`
- `.codex/plans/2026-05-30-team-console-discovery-requirements.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08d-generated-task-observer-plan.md`
- `apps/team-console/README.md`
- `docs/team-runtime.md`
- `docs/change-log.md`
- `apps/team-console/src/app/App.tsx`
- `apps/team-console/src/app/use-task-branch-stack.ts`
- `apps/team-console/src/app/use-team-console-live-data.ts`
- `apps/team-console/src/graph/ExecutionMap.tsx`
- `apps/team-console/src/graph/execution-map.css`
- `apps/team-console/src/fixtures/team-fixtures.ts`
- `apps/team-console/src/tests/app-live-data.test.tsx`
- `apps/team-console/src/tests/app-run-observer.test.tsx`
- `apps/team-console/src/tests/app-run-observer-detail.test.tsx`

严格按计划文件执行：
- `.codex/plans/2026-05-31-team-console-discovery-step-08d-generated-task-observer-plan.md`

本轮只做：
- 在 Discovery 子画布 generated child card 内增加 run / stop / latest-run observer 操作。
- run/stop 使用现有 `runTask(generatedTask)` 和 `cancelTaskRun(generatedTask, activeRun)`，不要新增 API。
- generated run observer 复用现有 `taskRunObserverByRunId`、`listTaskRunAttempts()`、`readTaskRunAttemptFile()` 管线。
- generated observer panel 从 Discovery subcanvas panel 派生，显示 Worker / Checker process、attempt file rows、file detail。
- generated child 仍不进入 root `tasks`、root `tasksById`、root `taskNodes` 或 root canvas。
- 更新 focused tests、5174 docs 和 change-log。

禁止做：
- 不改 backend routes/store/validation/runtime runner/dispatcher/scheduler/Team worker。
- 不改 `.pi/skills/**`、runtime skill、`skills-lock.json`。
- 不改主 `/playground` 产品 UI。
- 不把 generated child Tasks 渲染成 root canvas cards。
- 不把 generated child 塞进 root `tasksById` / `taskNodes` 冒充 root Task。
- 不实现 generated child edit/archive/delete/reset-to-managed/dispatch error diagnostics；这些留给 Step 08E。
- 不创建第二套 run observer fetch pipeline。
- 不创建计划外文件；如果确实需要新测试文件，先说明原因并做 untracked trailing whitespace/EOL 检查。
- 不做整文件格式化或换行符转换；保持 touched files 既有 EOL/格式。
- 不 stage、不 commit、不 push。
- 不使用 `git add -A`。
- 不提交 `.env`、`.data`、runtime 产物、temp 文件、截图、报告产物、未知 `.pi/skills/*/skills-lock.json`。

执行要求：
- 这是 Step 08D，只做 generated child run/cancel + observer/file detail，完成后停下交付。
- 行为变更先补 tests，再写实现。
- 遇到计划外问题先停下说明，不要顺手扩范围。
- 如果小改动产生超大 diff，先检查是否 EOL/formatter churn，修正后再继续。
- UI 任务必须在真实入口 `http://127.0.0.1:5174/` 做浏览器验证，交付 measured evidence；不要让用户手动验证。
- 不允许宽泛 suppress console warning；必须修根因或精确限定已知 warning。

最终验证：
- `npm --prefix apps/team-console test -- --run src/tests/app-live-data.test.tsx src/tests/app-run-observer.test.tsx src/tests/app-run-observer-detail.test.tsx`
- `npm --prefix apps/team-console test -- --run src/tests/execution-map-ui.test.tsx src/tests/team-contract-drift.test.ts`
- `npm --prefix apps/team-console exec tsc -- --noEmit -p tsconfig.json`
- `npm --prefix apps/team-console run build`
- `npm --prefix apps/team-console test`
- `npx tsc --noEmit`
- `git diff --check`
- `git diff --stat` / `git diff --numstat`，确认没有异常大规模格式噪声
- `git ls-files --eol <touched files>`
- 如果有新 untracked 文件，跑 `Select-String -LiteralPath <new-file> -Pattern '[ \t]+$'` 和 `git ls-files --eol --others --exclude-standard <new-file>`
- `git diff --cached --stat`

浏览器验证必须报告：
- URL：`http://127.0.0.1:5174/`
- data source：mock / 示例数据
- 操作：点击 `发现云服务候选`，点击 `Discovery 子画布`，在 `task_generated_vultr` card 点击 run，打开 latest run observer，再点击 `worker-output-001.md`
- `[data-canvas-kind="discovery"]` count
- `[data-discovery-subcanvas-for="task_discovery_cloud_vendors"]` count
- panel 内 `[data-generated-task-id]` count
- `task_generated_vultr` run 后的 `data-generated-run-status`
- root canvas 内 `核查 Vultr 公开证据`、`核查 Hetzner 公开证据` root card matches 仍为 0
- `[data-generated-observer-task-id="task_generated_vultr"]` count
- Worker / Checker process node counts
- generated observer file rows count
- generated file detail panel count 和可见内容片段
- subcanvas panel / generated observer panel / file detail panel rects
- screenshot path，放在 `runtime/` 下，不要 stage

完成后按计划里的 Delivery Report Template 回复。不要 stage，不要 commit。
