请接手 E:\AII\ugk-pi，执行 `/playground/conn` 性能优化 Task 1。

计划文件：
- `.codex/plans/2026-05-22-playground-conn-performance-plan.md`

本步目标：
- Defer Editor Support Catalogs
- 首屏只加载 `/v1/conns`，不要提前加载 editor 用的 `/v1/agents`、`/v1/browsers`、`/v1/model-config`。

当前真实背景：
- `src/ui/conn-page-js.ts` 的 `loadData()` 当前同时 `Promise.all` 拉 conns、agents、browsers、model config。
- 首屏列表和详情摘要不需要完整 editor catalogs；这些目录主要用于 create/edit editor。
- 这一步不要处理 `/v1/conns/:connId/runs`，run history 延迟放到 Task 2。别贪，贪了 review 会变成考古。

必须先读：
- AGENTS.md
- docs/playground-current.md
- docs/runtime-assets-conn-feishu.md
- .codex/plans/2026-05-22-playground-conn-performance-plan.md
- src/ui/conn-page-js.ts
- src/ui/conn-page-css.ts
- src/ui/conn-page.ts
- test/server.test.ts
- test/conn-page-ui.test.ts

实现要求：
- 把 `loadData()` 拆成 conn list refresh 和 editor/support catalog loader。
- `init()` / first load 不得调用 `apiFetchAgentCatalog()`、`apiFetchBrowserCatalog()`、`apiFetchModelConfig()`。
- 打开 create/edit editor 时再加载 support catalogs。
- 成功加载后缓存 catalogs，后续 create/edit editor 复用缓存。
- catalog loading / unavailable 时，禁用保存或在 submit payload 前 guard，不能提交错误 provider/browser/agent 值。
- 手动刷新按钮仍应刷新当前 conn 列表；不必强制刷新 editor catalogs，除非 editor 正打开。
- 不改 conn runtime、route、worker、Feishu、artifact 行为。

测试先行：
- 先补测试证明 first-load `loadData()` 不再包含 agent/browser/model loaders。
- 补测试证明 `openEditor()` 或 editor 初始化路径会触发 support catalog loader。
- 补测试证明 support catalogs 成功后会被缓存复用。
- 补测试证明 catalog 未就绪时保存被禁用或 payload 被 guard。

验证：
- `node --test --import tsx test/server.test.ts`
- `node --test --import tsx test/conn-page-ui.test.ts`
- `npx tsc --noEmit`
- `git diff --check`
- 浏览器前先跑 `npm run docker:doctor`
- 浏览器打开 `http://127.0.0.1:3000/playground/conn`，首屏 waterfall 不应出现 `/v1/agents`、`/v1/browsers`、`/v1/model-config`
- 打开 create editor 后才请求这些 catalogs；再打开 edit editor 应复用缓存

提交：
- 一个 Task 一个 commit
- 建议 commit message: `perf(conn): defer editor support catalogs`
- 不要提交 `.env`、`.data`、`runtime/*`、`public/*`、截图、browser profile、`curate_news*.py`、`zhihu-hotlist-*.json`、reddit/qwen 临时报告、异常 session jsonl、无关 `.codex/plans`

交付报告请按计划文件 Delivery Report Template 回复。
