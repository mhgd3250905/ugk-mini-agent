请接手 E:\AII\ugk-pi 的 Playground chat performance Step 4：Lazy Non-Chat Data。

当前基线：
- 当前 HEAD: f31842e perf(playground): coalesce conversation catalog refreshes
- Step 1-3 已完成并审核通过。
- 工作区开始前必须 clean。

必须先读：
- AGENTS.md
- docs/playground-current.md
- docs/change-log.md
- .codex/plans/2026-05-22-playground-chat-performance-plan.md
- src/ui/playground.ts
- src/ui/playground-page-shell.ts
- src/ui/playground-conversations-controller.ts
- src/ui/playground-stream-controller.ts
- src/ui/playground-assets-controller.ts
- src/ui/playground-task-inbox-controller.ts
- src/ui/playground-conn-manager.ts
- src/ui/playground-agent-manager.ts
- test/server.test.ts

本轮只做：
- 延迟加载首屏聊天无关的数据和重型面板内容。
- 检查资产库、任务消息、conn、model config、browser workbench、agent manager 等是否在初始聊天入口被 eager load。
- 对不需要首屏的请求加 lazy gate：首次打开对应面板时加载，之后按已有刷新按钮或 stale 逻辑刷新。
- 保留必要的轻量 badge/summary，如果它们本来就是顶部 shell 的一部分。
- 补测试证明初始路径不 eager load 重型 panel 数据，打开对应面板仍会加载。
- 如改变用户可见加载行为，更新 docs/change-log.md；必要时更新 docs/playground-current.md。

禁止做：
- 不改 chat API。
- 不改 AgentService、session factory、Team Runtime、scheduler、worker、run workspace、conn worker。
- 不删除任何面板能力。
- 不启动 Chrome，不重启服务，除非明确需要真实浏览器验证。
- 不提交 .env/.data/runtime/public/截图/browser profile/curate_news*.py/未知 .pi/skills/*/skills-lock.json。
- 不做整文件格式化或 EOL 转换。

执行要求：
- 先定位初始加载链路，再改。
- 每个改动都要能解释为“减少首屏聊天入口的非聊天请求/渲染”。
- 如果发现某个请求其实是首屏必要状态，不要硬延迟，先说明。
- 不要使用 git add -A。

验证：
- node --test --import tsx test/server.test.ts
- 相关 controller test（如果新增或已有）
- npx tsc --noEmit
- git diff --check
- git diff --stat
- git diff --numstat
- git status --short --branch

建议浏览器/network 检查：
- 打开 http://127.0.0.1:3000/playground，确认首屏聊天入口减少非聊天请求。
- 打开文件库/任务消息/conn/model config/browser workbench，对应数据仍能加载。

提交建议：
- perf(playground): defer non-chat panel data loading

完成后交付：
- commit hash
- 改动文件与摘要
- 哪些请求从首屏延迟到面板打开
- 测试与浏览器验证结果
- 确认无 EOL/格式化噪音和无关文件提交
