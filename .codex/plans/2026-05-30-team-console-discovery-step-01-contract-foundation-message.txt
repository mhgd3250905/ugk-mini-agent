请接手 E:\AII\ugk-pi 的 Team Console Discovery 节点第 01 步：契约地基。

当前基线：
- 最新 commit: af0362a docs(team-console): record task chain validation
- 分支状态: main...origin/main [ahead 6]
- 已完成: Team Console Canvas Task / typed chain / task observer 基本完善；当前稳定 tag stable/team-console-task-chain-2026-05-30 已推送 GitHub 和 Gitee
- 当前验证: 上一轮已通过 Team Console focused/full tests、Team Console build、npx tsc --noEmit、git diff --check；本步仍必须自行复跑指定验证
- 暂存区: 应为空；执行前用 git diff --cached --stat 确认
- Dirty 边界: .pi/skills/anthropics/skill-creator/** 是既有 tracked deletion，不要 stage、不要恢复；.pi/skills/skill-creator/、.codex/plans/*、public/runtime 报告、临时 HTML、generate_report*.py、report_template.html 不要提交

必须先读：
- AGENTS.md
- apps/team-console/README.md
- docs/team-runtime.md
- .codex/plans/2026-05-30-team-console-discovery-requirements.md
- .codex/plans/2026-05-30-team-console-discovery-step-01-contract-foundation-plan.md
- src/team/types.ts
- src/team/public-contract.ts
- apps/team-console/src/api/team-types.ts
- apps/team-console/src/tests/team-contract-drift.test.ts

严格按计划文件执行：
- .codex/plans/2026-05-30-team-console-discovery-step-01-contract-foundation-plan.md

本轮只做：
- 添加 Discovery Task / generated Task 的共享 TypeScript 契约。
- 保持 backend/frontend TeamCanvasTask 类型兼容。
- 添加或更新 compile-time drift guard。
- 只处理契约地基，完成后停下交付。

禁止做：
- 不实现 runtime behavior、API route、UI、runner、scheduler、skill
- 不改 src/team/task-run-service.ts
- 不改 src/team/task-store.ts
- 不改 src/team/routes.ts
- 不改 src/team/agent-profile-role-runner.ts
- 不改 src/team/role-prompt-contract.ts
- 不改 src/team/orchestrator.ts
- 不改 apps/team-console/src/app/*
- 不改 apps/team-console/src/graph/*
- 不改 .pi/skills/**
- 不改主 /playground 产品 UI
- 不创建计划外文件
- 不做整文件格式化或换行符转换；保持 touched files 的既有 EOL/格式
- 不 stage、不 commit，除非用户另行明确授权
- 不使用 git add -A
- 不提交 .env/.data/runtime 产物/temp 文件/报告产物/未知 .pi/skills/*/skills-lock.json

执行要求：
- 新字段必须 additive/optional，不能破坏旧 persisted Task 数据。
- missing canvasKind 必须表示普通 root Task，不要把 canvasKind 做成必填字段。
- discoverySpec 只定义静态类型，不要在本步写校验或 route 行为。
- generatedSource 只定义静态类型，不要在本步写列表过滤、upsert、stale 标记或自动运行。
- TeamWorkUnitDefinition 可以新增 optional outputCheck?: TeamTaskOutputCheck，后续步骤才会把它接到 runner。
- 行为变更先补测试；本步没有行为变更，只允许类型和 drift guard。
- 遇到计划外问题先停下说明，不要顺手扩范围。
- 如果小改动产生超大 diff，先检查是否为 EOL/formatter churn，修正后再继续。

本步建议新增/同步的契约名：
- TeamCanvasTaskKind = "task" | "discovery"
- TeamGeneratedTaskItemStatus = "active" | "stale"
- TeamGeneratedTaskWorkUnitMode = "managed" | "customized"
- TeamDiscoverySpec
- TeamGeneratedTaskSource
- TeamCanvasTask.canvasKind?
- TeamCanvasTask.discoverySpec?
- TeamCanvasTask.generatedSource?
- TeamWorkUnitDefinition.outputCheck?

最终验证：
- npm --prefix apps/team-console test -- --run src/tests/team-contract-drift.test.ts
- 如果修改或新增了 test/team-types.test.ts: node --test --import tsx test/team-types.test.ts
- npm --prefix apps/team-console exec tsc -- --noEmit -p tsconfig.json
- npx tsc --noEmit
- git diff --check
- git diff --stat
- git diff --numstat
- git ls-files --eol src/team/types.ts src/team/public-contract.ts apps/team-console/src/api/team-types.ts apps/team-console/src/tests/team-contract-drift.test.ts
- git diff --cached --stat

完成后按这个格式回复：
1. 改了哪些文件。
2. 新增了哪些 public/shared contract。
3. 明确说明没有改 runtime behavior、API route、UI、runner、scheduler、skill。
4. 列出每条验证命令和结果。
5. 列出 git diff --stat / git diff --numstat 摘要，并说明是否存在异常大规模格式噪声。
6. 说明是否出现 EOL 或 formatter-only churn。
7. 说明是否有计划假设不成立。

不要 stage，不要 commit。
