请接手 `E:\AII\ugk-pi` 的 Team Console Discovery Step 08E2C：generated child archive/delete UI。

当前基线：
- 最新 commit：`af0362a docs(team-console): record task chain validation`
- 当前工作区已有本地 unstaged Discovery 08A-08E2B 改动；不要 stage、不要 commit。
- Codex 已审核 08E2B：可通过；唯一交付报告口径修正是 console 不是空，而是仅 `/favicon.ico` 404，无应用 runtime warning/error。
- 08E2B fresh verification 已通过：focused App live-data / ExecutionMap UI tests 156 passed；Team Console API/contract 90 passed；Team Console `tsc`、build、full Vitest 518 passed、top-level `npx tsc --noEmit`、`git diff --check` 均通过；`git diff --cached --stat` 为空。
- 既有 dirty 边界：`.pi/skills/anthropics/skill-creator/**` tracked deletions 不是本轮内容，别恢复、别 stage；`.pi/skills/skill-creator/`、`.codex/plans/*`、public/runtime 报告产物、临时 HTML/脚本都不要提交。

必须先读：
- `AGENTS.md`
- `.codex/plans/2026-05-30-team-console-discovery-requirements.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2c-generated-archive-delete-plan.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2a-generated-edit-reset-ui-plan.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2b-failed-dispatch-diagnostics-plan.md`
- `apps/team-console/src/api/team-api.ts`
- `apps/team-console/src/app/use-team-console-live-data.ts`
- `apps/team-console/src/app/App.tsx`
- `apps/team-console/src/fixtures/team-fixtures.ts`
- `apps/team-console/src/graph/execution-map.css`
- `apps/team-console/src/tests/app-live-data.test.tsx`
- `apps/team-console/README.md`
- `docs/team-runtime.md`
- `docs/change-log.md`

严格按计划文件执行：
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2c-generated-archive-delete-plan.md`

本轮只做：
- 在 Discovery 子画布 generated child card 上增加 scoped delete/archive action。
- 使用既有 `CanvasTaskGateway.archiveTask(taskId)` / `POST /v1/team/tasks/:taskId/archive` 做软归档。
- 成功后只从 `generatedTasksByDiscoveryTaskId[sourceDiscoveryTaskId]` 移除该 generated child，并更新 root Discovery summary counts。
- 归档 generated child 时清理它自己的 edit/observer/file-detail 状态，但保持 root Discovery branch 和 Discovery 子画布打开。
- 更新 focused tests 和 5174 docs/change-log/requirements 状态。

禁止做：
- 不新增 backend endpoint。
- 不做 hard delete、物理删除、restore/unarchive。
- 不改 `src/team/**` backend routes/store/runtime/dispatcher/scheduler。
- 不改 Discovery dispatch、generated upsert、auto-run scheduler、`TeamDiscoveryDispatchOutcome` schema。
- 不创建 generated Tasks from diagnostics。
- 不改 generated reset/edit 语义，除非是归档后清理该 generated child 的 UI 状态。
- 不把 generated child 塞进 root `tasks` / `tasksById` / `taskNodes` / root canvas。
- 不关闭 root Discovery branch，不归档 root Discovery Task。
- 不把 blocked diagnostics 当成可删除 generated child。
- 不碰主 `/playground`。
- 不碰 `.pi/skills/**`、runtime skill、`skills-lock.json`。
- 不创建计划外文件。
- 不做整文件格式化或换行符转换；保持 touched files 既有 EOL/格式。
- 不 stage、不 commit、不 push。
- 不使用 `git add -A`。

执行要求：
- 这是 Step 08E2C，只处理 generated child soft archive/delete UI，完成后停下交付。
- 行为变更先补 tests，再写实现；先确认 RED。
- 不要直接复用 `App.archiveTask(task, nodeId?)` 来处理 generated child；那个 helper 是 root Task 清理用的，会刷新 root tasks 并关闭 root branch。generated child 需要独立 helper，只更新 generated catalog 和相关子画布状态。
- 成功归档后 root Discovery summary 的 generated/active/stale counts 要反映剩余非 archived generated children；blocked dispatch count 不变。
- 失败时显示现有 error banner，generated card 和 Discovery 子画布都保留。
- UI/交互任务必须在真实入口 `http://127.0.0.1:5174/` 做浏览器验证，交付 measured evidence；不要把可自动验证的步骤丢给用户手动做。
- 不允许宽泛 suppress console warning；已知 `/favicon.ico` 404 如果出现，报告为无关资源 404，不要谎称 console 空。

测试至少覆盖：
- Mock happy path：打开 `Discovery 子画布`，`[data-generated-task-id]` 从 2 变 1；被归档的 `task_generated_vultr` 消失，`task_generated_hetzner` 仍在；root summary 从 `2 items / 1 active / 1 stale / 1 blocked` 变为 `1 items / 0 active / 1 stale / 1 blocked`；root canvas generated title matches 仍为 0。
- State cleanup：归档正在编辑或观察的 generated child 后，`[data-generated-edit-task-id="<taskId>"]` 和该 generated observer/file detail 消失，但 Discovery 子画布仍打开。
- Live endpoint：点击 generated child archive/confirm 后调用 `POST /v1/team/tasks/task_generated_vultr/archive`，不新增 endpoint，不把 generated child 渲染成 root card。
- Failure path：archive endpoint 失败时 error banner 可见，generated card 保留，Discovery 子画布保留，count 不变。

最终验证：
- `npm --prefix apps/team-console test -- --run src/tests/app-live-data.test.tsx`
- `npm --prefix apps/team-console test -- --run src/tests/team-api.test.ts src/tests/team-contract-drift.test.ts`
- `npm --prefix apps/team-console exec tsc -- --noEmit -p tsconfig.json`
- `npm --prefix apps/team-console run build`
- `npm --prefix apps/team-console test`
- `npx tsc --noEmit`
- `git diff --check`
- `git diff --stat`
- `git diff --numstat`
- `git ls-files --eol apps/team-console/src/app/App.tsx apps/team-console/src/app/use-team-console-live-data.ts apps/team-console/src/graph/execution-map.css apps/team-console/src/tests/app-live-data.test.tsx apps/team-console/README.md docs/team-runtime.md docs/change-log.md .codex/plans/2026-05-30-team-console-discovery-requirements.md`
- `git diff --cached --stat`

浏览器验证必须报告：
- URL：`http://127.0.0.1:5174/`
- data source：mock / 示例数据
- `[data-canvas-kind="discovery"]` count
- archive 前 root Discovery summary：`2 items`、`1 active`、`1 stale`、`1 blocked`
- archive 前 `[data-generated-task-id]` count：2
- 点击 `task_generated_vultr` generated child 的 archive/delete action 并确认
- archive 后 `[data-generated-task-id]` count：1
- `task_generated_vultr` absent，`task_generated_hetzner` visible
- archive 后 root Discovery summary：`1 items`、`0 active`、`1 stale`、`1 blocked`
- root canvas generated title matches 仍为 0
- Discovery 子画布仍打开
- screenshot path，放在 `runtime/` 下，不要 stage
- console issues；如果只有 favicon 404，要如实写，不要写 `[]`

如果 5174 还在跑旧代码，重启 Team Console dev service/container 后再验；不要让用户跑 console snippet。

完成后按计划里的 Delivery Report Template 回复。不要 stage，不要 commit。
