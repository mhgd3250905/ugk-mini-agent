请接手 E:\AII\ugk-pi，执行 `/playground/conn` 性能优化 Task 6。

计划文件：
- `.codex/plans/2026-05-22-playground-conn-performance-plan.md`

本步目标：
- Improve Loading States Without Visual Redesign
- 在 run history 懒加载、分页和错误时提供清晰状态，但不做页面视觉大改。

当前真实背景：
- 前几步会把完整 runs 从首屏挪到用户需要时加载，并改成分页。
- 如果没有 loading/empty/error/has-more 状态，用户会误以为页面坏了。
- 这个页面已经有 cockpit 视觉系统；别再塞一堆新卡片和说明文，页面不是产品发布会。

必须先读：
- AGENTS.md
- DESIGN.md
- docs/playground-current.md
- .codex/plans/2026-05-22-playground-conn-performance-plan.md
- src/ui/conn-page-js.ts
- src/ui/conn-page-css.ts
- test/server.test.ts
- test/conn-page-ui.test.ts

实现要求：
- 添加 run history compact loading state。
- 添加 run history error + retry。
- 添加 loaded empty state。
- 添加 has-more 和 loading-more state。
- retry 必须使用当前 selected conn id，不能把旧 conn 的错误重试到新 conn。
- 深色/浅色主题都必须走现有 token，不要把颜色写死成单主题。
- 不加装饰性卡片、不写营销说明、不改变 conn runtime。

测试先行：
- 测试页面脚本包含 loading、empty、error、retry、has-more 状态路径。
- 测试 retry 有 selectedId guard。
- 如触碰 CSS，补类名/token 断言。

验证：
- `node --test --import tsx test/server.test.ts`
- `node --test --import tsx test/conn-page-ui.test.ts`
- `npx tsc --noEmit`
- `git diff --check`
- 浏览器在深色和浅色主题都检查 run history loading/error/empty/has-more。
- 状态切换不应导致 detail panel 大幅跳动。

提交：
- 一个 Task 一个 commit。
- 建议 commit message: `ux(conn): add bounded run history loading states`
- 不要提交无关运行态文件。

交付报告请按计划文件 Delivery Report Template 回复。
