请接手 E:\AII\ugk-pi，执行 `/playground/agents` Agent 管理页性能优化。

当前基线：
- 最新 commit: 8b1c5ee perf(playground): defer non-chat panel data loading
- 当前真实测量显示：慢点不是 `/v1/agents`，而是重复 `/v1/agents/main/skills`、技能全量 DOM、Agent 切换时技能请求等待和整块重渲染
- 当前工作区有无关未跟踪 `.codex/plans/2026-05-22-playground-chat-performance*` 文件，不要提交

必须先读：
- AGENTS.md
- docs/playground-current.md
- docs/change-log.md
- .codex/plans/2026-05-22-playground-agents-performance-plan.md
- src/ui/agents-page.ts
- src/routes/agent-profiles.ts
- src/agent/agent-profile-catalog.ts
- src/agent/agent-service-registry.ts
- test/server.test.ts
- test/playground-agent-switch.test.ts

严格按计划文件执行：
- .codex/plans/2026-05-22-playground-agents-performance-plan.md

本轮只做：
- 去掉首屏重复 `/v1/agents/main/skills`
- 技能区 lazy render，避免首屏挂满技能开关
- per-agent skill cache，切换回来不重复拉取
- 延迟 browsers/model-config 到新建/编辑 Agent 时加载
- 拆分目标区域渲染，减少整块 detail 重绘
- 补真实行为测试、浏览器验证和文档记录

禁止做：
- 不改 Agent runtime 执行语义
- 不改 profile 创建/归档/技能启停的后端规则，除非计划明确要求且保持兼容
- 不直接编辑 .data/agents/profiles.json
- 不改 Team profile lock 语义
- 不做视觉大改版
- 不做整文件格式化或 EOL 转换
- 不提交 .env、.data、runtime/*、public/*、截图、浏览器 profile、curate_news*.py、无关 .codex/plans

执行要求：
- 一个 Task 一个 commit
- 每个 Task 先补测试，再写实现
- 测试必须证明真实行为，不要只断言函数名存在
- 小改动如果出现超大 diff，先停下检查 EOL/formatter churn
- 遇到计划假设不成立，先汇报，不要自己扩范围

最终验证：
- node --test --import tsx test/server.test.ts
- node --test --import tsx test/playground-agent-switch.test.ts
- npx tsc --noEmit
- git diff --check
- git diff --stat
- git diff --numstat
- git status --short --branch
- 浏览器验证 `/playground/agents` 首屏、Agent 切换、技能展开、编辑器打开的网络瀑布

完成后按计划文件里的 Delivery Report Template 回复。
