请接手 `E:\AII\ugk-pi` 的 Team Console Discovery Step 08E2A：generated child light edit + reset-to-managed UI。

当前基线：
- 分支：`main...origin/main [ahead 6]`
- 最新 commit：`af0362a docs(team-console): record task chain validation`
- Discovery 已完成到 Step 08E1：generated child catalog / run / cancel / latest-run observer / file detail 已接入；backend/shared contract/API adapter 已有 `latestManagedWorkUnit` 和 `resetGeneratedTaskWorkUnit(taskId)`。
- Step 08E1 Codex 复核通过：store 27 passed、routes 37 passed、Team Console API/contract 90 passed、`app-live-data.test.tsx` 35 passed、Team Console `tsc`、top-level `npx tsc --noEmit`、`git diff --check` 均通过。
- 暂存区应为空；不要 stage，不要 commit。
- 既有 dirty 边界：`.pi/skills/anthropics/skill-creator/**` tracked deletions 不是本轮内容，别恢复、别 stage；`.pi/skills/skill-creator/`、`.codex/plans/*`、public/runtime 报告产物、临时 HTML/脚本都不要提交。

必须先读：
- `AGENTS.md`
- `.codex/plans/2026-05-30-team-console-discovery-requirements.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e1-generated-reset-contract-plan.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2a-generated-edit-reset-ui-plan.md`
- `apps/team-console/README.md`
- `docs/team-runtime.md`
- `docs/change-log.md`
- `apps/team-console/src/app/App.tsx`
- `apps/team-console/src/app/use-task-branch-stack.ts`
- `apps/team-console/src/app/use-team-console-live-data.ts`
- `apps/team-console/src/api/team-api.ts`
- `apps/team-console/src/api/team-types.ts`
- `apps/team-console/src/fixtures/team-fixtures.ts`
- `apps/team-console/src/graph/execution-map.css`
- `apps/team-console/src/tests/app-live-data.test.tsx`
- `apps/team-console/src/tests/team-api.test.ts`

严格按计划文件执行：
- `.codex/plans/2026-05-31-team-console-discovery-step-08e2a-generated-edit-reset-ui-plan.md`

本轮只做：
- 在 Discovery 子画布 generated child card 内增加 light edit 入口。
- generated edit panel 必须从 Discovery subcanvas panel 派生，暴露 `data-generated-edit-task-id="<generatedTaskId>"`。
- light edit 只允许修改 Task 名称、Leader Agent、Worker Agent、Checker Agent。
- generated Task title 变更必须同时 patch visible `title` 和 `workUnit.title`，让后端把 `workUnitMode` 标记为 `customized`；别搞成 rerun 下一次又把用户标题冲掉。
- 在 customized 且存在 `latestManagedWorkUnit` 的 generated child card 上增加 reset-to-managed 入口：`data-generated-action="reset-workunit"`。
- reset 调用现有 `api.resetGeneratedTaskWorkUnit(taskId)`，更新 generated catalog state，保持 Discovery 子画布打开。
- 更新 focused tests、5174 docs 和 change-log。

禁止做：
- 不做 failed dispatch diagnostics；这个拆到 Step 08E2B。
- 不改 backend routes/store/validation/runtime runner/dispatcher/scheduler/Team worker。
- 不改 `.pi/skills/**`、runtime skill、`skills-lock.json`。
- 不改主 `/playground` 产品 UI。
- 不把 generated child Tasks 渲染成 root canvas cards。
- 不把 generated child 塞进 root `tasksById` / `taskNodes` 冒充 root Task。
- 不实现 generated child archive/delete。
- 不创建新后端 endpoint；reset endpoint 已经存在。
- 不创建计划外文件；如果确实需要新测试文件，先说明原因并做 untracked trailing whitespace/EOL 检查。
- 不做整文件格式化或换行符转换；保持 touched files 既有 EOL/格式。
- 不 stage、不 commit、不 push。
- 不使用 `git add -A`。
- 不提交 `.env`、`.data`、runtime 产物、temp 文件、截图、报告产物、未知 `.pi/skills/*/skills-lock.json`。

执行要求：
- 这是 Step 08E2A，只做 generated child edit/reset UI，完成后停下交付。
- 行为变更先补 tests，再写实现。
- 遇到计划外问题先停下说明，不要顺手扩范围。
- 如果小改动产生超大 diff，先检查是否 EOL/formatter churn，修正后再继续。
- UI 任务必须在真实入口 `http://127.0.0.1:5174/` 做浏览器验证，交付 measured evidence；不要让用户手动验证。
- 不允许宽泛 suppress console warning；必须修根因或精确限定已知 warning。

测试至少覆盖：
- Mock generated edit：打开 `发现云服务候选` -> `Discovery 子画布` -> `task_generated_vultr`，点击 `[data-generated-action="edit"]`，出现 `[data-generated-edit-task-id="task_generated_vultr"]`，改标题并保存后 card title 更新、`data-generated-workunit-mode="customized"`，root canvas 仍没有 generated root card。
- Mock generated reset：`task_generated_hetzner` 初始 customized 且有 snapshot，点击 `[data-generated-action="reset-workunit"]` 后 title 恢复到 `latestManagedWorkUnit.title`，mode 变 `managed`，source item id/status 不变。
- Live reset：fetch mock 里点击 reset 后必须出现 `POST /v1/team/tasks/task_generated_hetzner/generated-workunit/reset`，返回 `{ task, warnings: [] }` 后 DOM 变 managed，generated child 仍不进 root canvas。
- Error path：edit 或 reset API 失败时显示错误，Discovery subcanvas 和对应 panel 不被关闭。

最终验证：
- `npm --prefix apps/team-console test -- --run src/tests/app-live-data.test.tsx`
- `npm --prefix apps/team-console test -- --run src/tests/team-api.test.ts src/tests/team-contract-drift.test.ts`
- `npm --prefix apps/team-console exec tsc -- --noEmit -p tsconfig.json`
- `npm --prefix apps/team-console run build`
- `npm --prefix apps/team-console test`
- `npx tsc --noEmit`
- `git diff --check`
- `git diff --stat` / `git diff --numstat`，确认没有异常大规模格式噪声
- `git ls-files --eol <touched files>`
- 如果有新 untracked 文件，跑 `Select-String -LiteralPath <new-file> -Pattern '[ \t]+$'` 和 `git ls-files --eol --others --exclude-standard <new-file>`
- `git diff --cached --stat`

浏览器验证必须报告：
- URL：`http://127.0.0.1:5174/`
- data source：mock / 示例数据
- 操作：点击 `发现云服务候选`，点击 `Discovery 子画布`，在 `task_generated_vultr` card 点击 edit，改标题并保存；在 `task_generated_hetzner` card 点击 reset-to-managed。
- `[data-canvas-kind="discovery"]` count
- `[data-discovery-subcanvas-for="task_discovery_cloud_vendors"]` count
- panel 内 `[data-generated-task-id]` count
- `[data-generated-edit-task-id="task_generated_vultr"]` count
- `task_generated_vultr` edit 后的 title 和 `data-generated-workunit-mode`
- `task_generated_hetzner` reset 后的 title 和 `data-generated-workunit-mode`
- root canvas 内 generated title matches 仍为 0
- Discovery subcanvas panel / generated edit panel rects
- screenshot path，放在 `runtime/` 下，不要 stage

完成后按计划里的 Delivery Report Template 回复。不要 stage，不要 commit。
