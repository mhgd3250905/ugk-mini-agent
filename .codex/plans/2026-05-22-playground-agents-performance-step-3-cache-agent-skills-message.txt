请接手 E:\AII\ugk-pi，执行 /playground/agents 性能优化 Task 3。

当前基线：
- 分支：main
- HEAD：0831db7 chore(docker): detect shadow localhost port
- Step 1 已完成：首屏只请求一次 `GET /v1/agents/main/skills`，并复用为 `skillsByAgentId.main`
- Step 2 已完成：selected Agent skills 默认折叠，点击“查看技能”后才挂载 rows/switches
- Step 2 review 已完成：未加载技能数显示 `—`，已加载空数组显示 `0`
- main ahead origin/main 15，未 push
- tracked diff 应为空
- `.codex/plans/*` 有未跟踪计划文件，保留不动，不要提交

计划文件：
- `.codex/plans/2026-05-22-playground-agents-performance-plan.md`

本步目标：
- Cache Per-Agent Skills With Explicit Refresh
- Agent 技能列表按 `agentId` 缓存；已加载过的 Agent 再次展开/切回时不应无意义重拉 `/v1/agents/:agentId/skills`
- 只有用户手动刷新，或 install/remove/toggle 这类技能 mutation 后，才刷新/失效对应 agent 的 skills

真实测试背景：
- 历史基线：切换到 `agent-patent-excavator` 时 `GET /v1/agents/agent-patent-excavator/skills` 约 `419.4ms`
- 当前 Step 2 后真实浏览器验证：
  - main 首屏：约 `253` DOM nodes，`.ag-skill-item = 0`，skill count `47`
  - search 折叠态：`.ag-skill-item = 0`，skill count `—`
  - search 展开后：`.ag-skill-item = 6`，skill count `6`
  - 展开前 skills requests 只有 `/v1/agents/main/skills`
  - 点击 search 的“查看技能”后才请求 `/v1/agents/search/skills`
- 本步要验证：search 已展开加载后，切走再切回 search，不应再次请求 `/v1/agents/search/skills`，除非点击“刷新”或发生 mutation

必须先读：
- `AGENTS.md`
- `docs/playground-current.md`
- `docs/change-log.md`
- `.codex/plans/2026-05-22-playground-agents-performance-plan.md`
- `src/ui/agents-page.ts`
- `src/routes/agent-profiles.ts`
- `test/server.test.ts`
- `test/playground-agent-switch.test.ts`
- 相关 agent-profile tests

实现要求：
- 添加 per-agent skill cache metadata，例如 `skillsLoadedByAgentId`、`skillsCacheStateByAgentId` 或等价结构
- 保留 Step 2 lazy-render 行为：`selectAgent(agentId)` 不应因为切换 Agent 就主动 fetch skills
- skills section 打开时：
  - 如果该 agent skills 已加载，直接渲染缓存
  - 如果未加载，再调用现有 `apiFetchAgentSkills(agentId)`
- revisiting loaded agent 时：
  - 不重复请求 `/v1/agents/:agentId/skills`
  - 不显示错误 loading 状态
  - 不把“已加载空数组”误判成“未加载”
- 已加载空数组必须是有效 cache，不能因为 `length === 0` 就再次 fetch
- `apiFetchGallerySkills()` 对 main 的缓存语义必须保留：main gallery result 继续写入 main scoped skills cache
- install/remove/toggle 后，只刷新或失效当前 touched agent，不要清空所有 agent cache
- 手动“刷新技能”必须 force refetch 当前 selected agent
- 如需给 `apiFetchAgentSkills()` 增加 `force` 参数，必须保持旧调用向后兼容
- 不改 Agent runtime、profile 创建/归档、技能 enable/disable/delete/copy/install/refresh API 语义
- 不编辑 `.data/agents/profiles.json`

禁止做：
- 不做 Step 4：不要延迟 `/v1/browsers` 或 `/v1/model-config`
- 不做 Step 5：不要拆 stable detail sections 或大改 render 架构
- 不做页面视觉重设计
- 不新增后端接口，除非已有测试证明前端无法完成；默认只改 `src/ui/agents-page.ts`
- 不运行 broad formatter，不做 EOL 归一化
- 不提交 `.env`、`.data`、`runtime/*`、`public/*`、截图、browser profile、`curate_news*.py`、未知 `.pi/skills/*`、`skills-lock.json`、无关 `.codex/plans`

测试先行：
- 先补失败测试，再实现；不要先改源码再补测
- 测试 revisiting loaded agent 不 refetch skills
- 测试未加载 agent 只有打开 skills section 时才 fetch
- 测试已加载空数组是有效 cache，不会因为 `length === 0` 再 fetch
- 测试 manual refresh 仍 force fetch selected agent
- 测试 toggle/remove/install 只刷新或失效 selected agent，不影响其他 agent cache
- 测试 main 的 gallery skills 仍写入 main cache，且不会破坏 Step 1 去重
- 测试不要只查字符串存在；至少锁定关键函数/状态分支，能防止“切回来又 fetch”

建议测试锚点：
- `test/server.test.ts` 现有 `/playground/agents` 脚本断言附近
- 可通过切片检查 `handleExpandSkills()`、`selectAgent()`、`handleRefreshSkills()`、`handleCopySkill()`、`handleRemoveSkill()`、toggle callback 的 cache/force/invalidation 分支
- 如能低成本 eval 页面脚本函数，可优先用行为级测试，而不是纯字符串测试

验证：
```powershell
npm run docker:doctor
node --test --import tsx test\server.test.ts
node --test --import tsx test\playground-agent-switch.test.ts
npx tsc --noEmit
git diff --check
git diff --stat
git status --short --branch
```

浏览器真实验证：
- 先运行 `npm run docker:doctor`，必须通过；避免 `127.0.0.1:3000` 被宿主 Node 截胡
- reload `http://127.0.0.1:3000/playground/agents`
- 确认首屏只有一次 `/v1/agents/main/skills`
- 选择 Agent A（例如 search），点击“查看技能”，等 A skills 出现
- 切到 Agent B，点击“查看技能”，等 B skills 出现
- 再切回 Agent A 并打开 skills：不应再次请求 A 的 `/skills`
- 点击“刷新”后，才应重新请求当前 selected agent 的 `/skills`
- 做一次 toggle/copy/remove 的非破坏性验证时要谨慎，优先用测试覆盖 mutation 语义；浏览器不要随便改真实运行数据

提交：
- 一个 Task 一个 commit
- 建议 commit message: `perf(agents): cache scoped skills per agent`
- 提交前检查 `git diff --stat` / `git diff --numstat`，如果小改动出现大规模 diff，先停下排查 EOL/formatter churn
- 不要提交无关脏文件和未跟踪 `.codex/plans/*`

交付报告请按计划文件 Delivery Report Template 回复：
- Commit
- Files changed
- Behavior changed
- Tests
- Browser verification
- EOL / formatting
- Dirty workspace
- Known residual risk
