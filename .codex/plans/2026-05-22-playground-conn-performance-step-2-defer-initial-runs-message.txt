请接手 E:\AII\ugk-pi，执行 `/playground/conn` 性能优化 Task 2。

计划文件：
- `.codex/plans/2026-05-22-playground-conn-performance-plan.md`

本步目标：
- Stop Auto-Fetching Full Runs On First Load
- 首屏加载 `/playground/conn` 时，不要自动请求第一条任务的完整 run history。
- 保留任务列表、统计、选中/摘要状态，但 run history 只在用户明确需要时加载。

当前真实背景：
- `init()` 当前 `loadData().then(...)` 后会自动 `handleConnSelect(state.conns[0].connId)`。
- `handleConnSelect()` 当前在 `runsByConnId[connId]` 不存在时调用 `apiFetchRuns(connId)`。
- `/v1/conns` 已返回 `latestRun` 摘要，首屏不必拉完整 runs。
- Task 1 已负责 editor catalogs；本步只处理 run history 首屏加载。

必须先读：
- AGENTS.md
- docs/playground-current.md
- docs/runtime-assets-conn-feishu.md
- .codex/plans/2026-05-22-playground-conn-performance-plan.md
- src/ui/conn-page-js.ts
- src/ui/conn-page-css.ts
- src/ui/conn-page.ts
- src/routes/conns.ts
- src/routes/conn-route-presenters.ts
- test/server.test.ts
- test/conn-page-ui.test.ts

实现要求：
- 允许首屏视觉上选中第一条 conn，但不得自动调用 `apiFetchRuns(firstConnId)`。
- 使用 `/v1/conns` 返回的 `conn.latestRun` 渲染首屏摘要。
- 增加 `runsLoadedByConnId`、`runHistoryStateByConnId` 或等价状态，区分“未加载”“加载中”“已加载空数组”“加载失败”。
- run history 区域提供明确懒加载入口，例如“加载运行历史”。
- 用户点击懒加载入口后才请求当前 conn 的 runs。
- 异步返回前如果用户已切换 selected conn，不得把旧 conn 的 runs 画到新面板。
- 不改变 conn worker、scheduler、lease、workspace、artifact、Feishu 行为。

测试先行：
- 先补测试证明 `init()` / first-load 不再自动链到 `apiFetchRuns(firstConnId)`。
- 补测试证明首屏可用 `latestRun` 渲染摘要，不依赖完整 runs。
- 补测试证明点击懒加载入口才请求 runs。
- 补测试证明 loaded empty array 是有效缓存，不应退回未加载。
- 补测试证明 async result 需要 selectedId guard。

验证：
- `node --test --import tsx test/server.test.ts`
- `node --test --import tsx test/conn-page-ui.test.ts`
- `npx tsc --noEmit`
- `git diff --check`
- 浏览器前先跑 `npm run docker:doctor`
- 浏览器打开 `http://127.0.0.1:3000/playground/conn`，首屏 waterfall 不应出现 `/v1/conns/:connId/runs`
- 页面仍显示任务列表、统计、选中状态和摘要，不出现破碎空面板

提交：
- 一个 Task 一个 commit
- 建议 commit message: `perf(conn): defer initial run history loading`
- 不要提交无关脏文件或未跟踪运行态

交付报告请按计划文件 Delivery Report Template 回复。
