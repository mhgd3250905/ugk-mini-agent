请按 `E:\AII\ugk-pi\.codex\plans\2026-05-31-team-console-discovery-step-08e1-generated-reset-contract-plan.md` 执行 Step 08E1。

本步目标：只做 generated Task reset-to-managed 的 contract/store/route/API adapter 基础，不做 5174 UI。现在 `workUnitMode="customized"` 后没有保存 latest managed WorkUnit，前端 reset 按钮没有可信恢复来源；别硬做 UI 自欺欺人。

当前 baseline：

- 仓库：`E:\AII\ugk-pi`
- 分支：`main`
- 状态：`main...origin/main [ahead 6]`
- 最新提交：`af0362a docs(team-console): record task chain validation`
- Step 08D 已完成并由 Codex 复核：generated child run/cancel、latest-run observer、Worker/Checker process、file detail 均通过；generated child 没有进入 root canvas。
- 暂存区必须保持为空。

开始前必须读：

- `AGENTS.md`
- `.codex/plans/2026-05-30-team-console-discovery-requirements.md`
- `.codex/plans/2026-05-31-team-console-discovery-step-08e1-generated-reset-contract-plan.md`
- `docs/team-runtime.md`
- `docs/change-log.md`
- `src/team/types.ts`
- `src/team/public-contract.ts`
- `src/team/task-validation.ts`
- `src/team/task-store.ts`
- `src/team/routes.ts`
- `apps/team-console/src/api/team-types.ts`
- `apps/team-console/src/api/team-api.ts`
- `apps/team-console/src/fixtures/team-fixtures.ts`
- `test/team-task-store.test.ts`
- `test/team-task-routes.test.ts`
- `apps/team-console/src/tests/team-api.test.ts`
- `apps/team-console/src/tests/team-contract-drift.test.ts`

开始前执行：

```powershell
git status --short --branch
git log -5 --oneline
git diff --stat
git diff --cached --stat
git remote -v
git log --oneline origin/main..HEAD
```

必须实现：

1. 给 `TeamGeneratedTaskSource` 增加可选 `latestManagedWorkUnit?: TeamWorkUnitDefinition`，后端类型、public contract、Team Console mirrored type 和 drift guard 同步。旧 generated Task 没有该字段必须继续可读。
2. `TaskStore.upsertGeneratedTaskFromDiscovery()` 创建/更新 generated Task 时保存最新 dispatcher-managed WorkUnit 到 `generatedSource.latestManagedWorkUnit`。
3. managed generated Task rerun：继续更新 visible `title/workUnit`，并更新 latest snapshot。
4. customized generated Task rerun：不覆盖 visible `title/workUnit`，但更新 `itemPayload`、latest discovery metadata 和 `latestManagedWorkUnit`。
5. public `TaskStore.update()` / `PATCH /v1/team/tasks/:taskId` 对 generated `workUnit` 的编辑继续把 `workUnitMode` 标记为 `customized`，且不能清掉 latest snapshot。
6. 新增 reset store 方法和 route，推荐 endpoint：

```text
POST /v1/team/tasks/:taskId/generated-workunit/reset
```

Reset 语义：

- 只允许 generated Task。
- archived generated Task 拒绝。
- 缺少 `latestManagedWorkUnit` 的旧 generated Task 拒绝，不能猜。
- reset 后 visible `workUnit = latestManagedWorkUnit`，visible `title = latestManagedWorkUnit.title`，`generatedSource.workUnitMode = "managed"`。
- 必须保留 `sourceDiscoveryTaskId/sourceItemId/itemPayload/itemStatus/latestDiscoveryRunId/latestDiscoveryAttemptId/latestDiscoveredAt/latestManagedWorkUnit`。
- 响应 shape 跟现有 Task mutation 一致：`{ task, warnings? }`。

7. Team Console API adapter 增加：

```ts
resetGeneratedTaskWorkUnit(taskId: string): Promise<TeamTaskMutationResponse>
```

`LiveTeamApi` 调 POST；`MockTeamApi` 真实更新 mock generated catalog，不能只返回假对象。

禁止做：

- 不做 5174 UI 按钮、面板或视觉样式。
- 不改 `apps/team-console/src/app/App.tsx`，除非类型检查证明非改不可；如果要改，先在交付报告里说明为什么。
- 不做 generated child light edit UI。
- 不做 dispatch diagnostics UI。
- 不 archive/delete generated Tasks。
- 不 rerun dispatcher。
- 不改 dispatcher prompt/parser、scheduler、runner、Plan orchestrator、主 `/playground`、`.pi/skills/**`。
- 不 stage，不 commit，不用 `git add -A`。

先写/改测试再实现。至少覆盖：

- `test/team-task-store.test.ts`
  - create stores latest snapshot
  - managed rerun updates visible WorkUnit and snapshot
  - customized rerun preserves visible WorkUnit but updates snapshot
  - public generated WorkUnit edit marks customized and preserves snapshot
  - reset restores snapshot and marks managed
  - reset rejects normal root / Discovery root / archived / generated without snapshot
- `test/team-task-routes.test.ts`
  - reset success and all rejection cases
  - public create/update still rejects `generatedSource`
- `apps/team-console/src/tests/team-api.test.ts`
  - Live POST path is encoded correctly
  - non-2xx maps through existing API error behavior
  - Mock reset updates generated catalog and preserves source identity
- `apps/team-console/src/tests/team-contract-drift.test.ts`
  - backend/frontend generated source contracts still match

验证命令：

```powershell
node --test --import tsx test/team-task-store.test.ts
node --test --import tsx test/team-task-routes.test.ts
npm --prefix apps/team-console test -- --run src/tests/team-api.test.ts src/tests/team-contract-drift.test.ts
npm --prefix apps/team-console exec tsc -- --noEmit -p tsconfig.json
npx tsc --noEmit
git diff --check
git diff --cached --stat
```

如果 Team Console API/types 影响超过预期，再补跑：

```powershell
npm --prefix apps/team-console test -- --run src/tests/app-live-data.test.tsx
```

本步不要求浏览器验证，因为明确不做 UI。若你碰了 UI 文件，那就必须停下来解释偏离原因，并补真实 `http://127.0.0.1:5174/` 行为证据。

交付报告按这个格式写：

1. Modified files。
2. Contract changes，写清 `TeamGeneratedTaskSource` 新字段。
3. Store behavior：create、managed rerun、customized rerun、public edit、reset。
4. Route/API behavior 和 exact endpoint。
5. Tests added/updated，以及每个测试抓的旧缺口。
6. Docs updated。
7. Explicit non-goals respected。
8. Verification results with pass counts。
9. Diff stat/numstat and EOL/formatter notes。
10. Staging status：确认 `git diff --cached --stat` 为空。
11. Any plan assumption that failed。
