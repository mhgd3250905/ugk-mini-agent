请接手 E:\AII\ugk-pi 的 Playground chat performance Step 6：Final Verification And Docs。

当前基线：
- Step 1-5 完成后再执行。

本轮只做：
- 汇总验证 Playground chat performance 改动。
- 补齐 docs/change-log.md 和必要的 docs/playground-current.md。
- 不再新增功能。

最终验证：
- npm test
- npx tsc --noEmit
- git diff --check
- git diff --stat
- git diff --numstat
- git status --short --branch

建议浏览器检查：
- /playground 首屏
- 进入 main Agent
- 会话切换
- desktop/mobile 会话列表滚动
- 打开文件库、任务消息、conn、model config、browser workbench

禁止做：
- 不改运行时核心。
- 不扩大范围。
- 不提交无关脏文件，不使用 git add -A。

提交建议：
- docs(playground): document chat performance refinements
